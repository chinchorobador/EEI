apiVersion: apps/v1
kind: Deployment
metadata:
  name: usuarios-memcached
spec:
  selector:
    matchLabels:
      app: usuarios-memcached
  template:
    metadata:
      labels:
        app: usuarios-memcached
    spec:
      containers:
        - name: memcached
          image: bitnami/memcached:1.6.21
          imagePullPolicy: Always
          resources:
            requests:
              cpu: 100m
              memory: 200Mi
---
apiVersion: v1
kind: Service
metadata:
  name: usuarios-memcached
spec:
  selector:
    app: usuarios-memcached
  ports:
    - port: 11211
      targetPort: 11211
