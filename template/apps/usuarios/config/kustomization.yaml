kind: Component

configMapGenerator:
  - name: usuarios.env
    envs:
      - usuarios.env
    behavior: merge
  - name: usuarios-api.env
    envs:
      - usuarios-api.env
    behavior: merge
  - name: usuarios-idp.env
    envs:
      - usuarios-idp.env
    behavior: merge